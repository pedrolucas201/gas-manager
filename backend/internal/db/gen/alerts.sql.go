// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: alerts.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const negativeStock = `-- name: NegativeStock :many
SELECT i.cylinder_type_id, ct.name, i.full_qty, i.empty_qty
FROM inventory i JOIN cylinder_types ct ON ct.id = i.cylinder_type_id
WHERE i.full_qty < 0 OR i.empty_qty < 0
`

type NegativeStockRow struct {
	CylinderTypeID pgtype.UUID
	Name           string
	FullQty        int32
	EmptyQty       int32
}

func (q *Queries) NegativeStock(ctx context.Context) ([]NegativeStockRow, error) {
	rows, err := q.db.Query(ctx, negativeStock)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []NegativeStockRow
	for rows.Next() {
		var i NegativeStockRow
		if err := rows.Scan(
			&i.CylinderTypeID,
			&i.Name,
			&i.FullQty,
			&i.EmptyQty,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const overLimitBalance = `-- name: OverLimitBalance :many
SELECT id, name, balance, credit_limit
FROM customers
WHERE credit_limit IS NOT NULL AND balance > credit_limit
`

type OverLimitBalanceRow struct {
	ID          pgtype.UUID
	Name        string
	Balance     pgtype.Numeric
	CreditLimit pgtype.Numeric
}

func (q *Queries) OverLimitBalance(ctx context.Context) ([]OverLimitBalanceRow, error) {
	rows, err := q.db.Query(ctx, overLimitBalance)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []OverLimitBalanceRow
	for rows.Next() {
		var i OverLimitBalanceRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Balance,
			&i.CreditLimit,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
