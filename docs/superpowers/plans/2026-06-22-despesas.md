# Despesas com Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Registrar despesas operacionais (gasolina, manutenção, pneu...) com sincronização entre todos os dispositivos via o backend Go existente.

**Architecture:** A tabela `expenses` é adicionada ao Postgres com os mesmos padrões dos outros fatos do sistema (UUID PK, sequence BIGSERIAL para cursor, payload_hash para idempotência). O cliente enfileira despesas no outbox como kind "expense" — elas entram no push em lote junto com vendas e reposições. O pull stream inclui um novo cursor `expense`. No mobile, migration v5 do SQLite cria a tabela local; eventos pullados são aplicados via `applyExpense` em `apply.ts`. A aba Financeiro exibe total de despesas do período.

**Tech Stack:** Go + pgx/v5 + sqlc v1.31.1 (backend), Expo SDK 54 + expo-sqlite + NativeWind (mobile)

> **Nota de deploy:** após implementar as tasks 1–7 (backend), executar `/backend` para deploy no Cloud Run antes de executar as tasks 8–13 (frontend). O pull só funciona com o backend atualizado.

---

## File Map

| Arquivo | Ação | Responsabilidade |
|---|---|---|
| `backend/internal/db/migrations/0006_expenses.up.sql` | CREATE | Tabela `expenses` no Postgres |
| `backend/internal/db/migrations/0006_expenses.down.sql` | CREATE | DROP da tabela |
| `backend/internal/db/queries/events.sql` | MODIFY | 3 queries sqlc: GetExpenseByID, InsertExpense, PullExpenses |
| `backend/internal/db/gen/events.sql.go` | AUTO-GENERATED | sqlc generate (não editar à mão) |
| `backend/internal/db/gen/models.go` | AUTO-GENERATED | sqlc generate |
| `backend/internal/sync/types.go` | MODIFY | `ExpensePayload` em `PushEvent` |
| `backend/internal/sync/pull_dto.go` | MODIFY | `ExpenseDTO` + `mapExpenseRow` |
| `backend/internal/sync/push.go` | MODIFY | existingHash + applyEvent para "expense" |
| `backend/internal/sync/pull.go` | MODIFY | `Cursor.Expense`, stream de expenses, avanço do cursor |
| `backend/internal/sync/testutil_test.go` | MODIFY | Adicionar `0006_expenses.up.sql` ao applyMigrations |
| `backend/internal/sync/expense_test.go` | CREATE | Testes de push + pull para expenses |
| `types/index.ts` | MODIFY | Interface `Expense` |
| `lib/api.ts` | MODIFY | `ExpensePayload` + `PushEvent` aceita "expense" |
| `db/database.ts` | MODIFY | Migration v5: tabela `expenses` no SQLite |
| `db/queries/expenses.ts` | CREATE | `addExpense`, `getExpenses` |
| `store/index.ts` | MODIFY | `expensesVersion` + `bumpExpenses` |
| `lib/sync/engine.ts` | MODIFY | "expense" em `FACT_KINDS` |
| `lib/sync/apply.ts` | MODIFY | `PulledExpense` + `applyExpense` |
| `app/add-expense.tsx` | CREATE | Formulário de nova despesa |
| `app/(tabs)/reports.tsx` | MODIFY | Seção "Despesas" na aba Financeiro |

---

## Task 1: Backend — Migration 0006

**Files:**
- Create: `backend/internal/db/migrations/0006_expenses.up.sql`
- Create: `backend/internal/db/migrations/0006_expenses.down.sql`

- [ ] **Step 1.1: Criar migration up**

```sql
CREATE TABLE expenses (
  id                 UUID PRIMARY KEY,
  category           TEXT NOT NULL,
  description        TEXT,
  amount             NUMERIC(12,2) NOT NULL,
  payload_hash       TEXT NOT NULL,
  created_by         TEXT NOT NULL REFERENCES users(id),
  client_created_at  TIMESTAMPTZ NOT NULL,
  server_received_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  sequence           BIGSERIAL NOT NULL
);

CREATE INDEX idx_expenses_seq ON expenses(sequence);
```

- [ ] **Step 1.2: Criar migration down**

```sql
DROP TABLE IF EXISTS expenses;
```

- [ ] **Step 1.3: Verificar que a migration sobe sem erro**

```powershell
# Usando a connection string do Supabase (production)
$env:PGPASSWORD = "<senha do DATABASE_URL no GCP Secret Manager>"
psql -h aws-1-sa-east-1.pooler.supabase.com -p 5432 -U postgres.aealxmiyotyeoutlqljy -d postgres -f backend/internal/db/migrations/0006_expenses.up.sql
```

Expected: `CREATE TABLE` + `CREATE INDEX` sem erros.

- [ ] **Step 1.4: Commit**

```bash
git add backend/internal/db/migrations/0006_expenses.up.sql backend/internal/db/migrations/0006_expenses.down.sql
git commit -m "feat(backend/db): migration 0006 — tabela expenses"
```

---

## Task 2: Backend — SQL queries + sqlc generate

**Files:**
- Modify: `backend/internal/db/queries/events.sql`
- Auto-generated: `backend/internal/db/gen/events.sql.go`

- [ ] **Step 2.1: Adicionar 3 queries ao final de `backend/internal/db/queries/events.sql`**

```sql
-- name: GetExpenseByID :one
SELECT id, payload_hash FROM expenses WHERE id = $1;

-- name: InsertExpense :one
INSERT INTO expenses (id, category, description, amount, payload_hash, created_by, client_created_at)
VALUES ($1,$2,$3,$4,$5,$6,$7)
RETURNING sequence, server_received_at;

-- name: PullExpenses :many
SELECT id, category, description, amount, server_received_at, sequence
FROM expenses WHERE sequence > $1 ORDER BY sequence LIMIT $2;
```

- [ ] **Step 2.2: Rodar sqlc generate**

```powershell
cd backend
sqlc generate
```

Expected: sem erros. Os arquivos `backend/internal/db/gen/events.sql.go` e `backend/internal/db/gen/models.go` são atualizados automaticamente com os novos tipos.

Se sqlc não estiver instalado: `go install github.com/sqlc-dev/sqlc/cmd/sqlc@v1.31.1` e tentar de novo.

Após o generate, o arquivo `events.sql.go` conterá automaticamente:

```go
// GetExpenseByID — retorna id + payload_hash para verificação de idempotência
type GetExpenseByIDRow struct {
    ID          pgtype.UUID
    PayloadHash string
}

// InsertExpenseParams — parâmetros para inserção
type InsertExpenseParams struct {
    ID              pgtype.UUID
    Category        string
    Description     *string  // nullable
    Amount          pgtype.Numeric
    PayloadHash     string
    CreatedBy       string
    ClientCreatedAt pgtype.Timestamptz
}

// InsertExpenseRow — retorno com sequence e server_received_at
type InsertExpenseRow struct {
    Sequence         int64
    ServerReceivedAt pgtype.Timestamptz
}

// PullExpensesParams — cursor e limit para o stream
type PullExpensesParams struct {
    Sequence int64
    Limit    int32
}

// PullExpensesRow — linha do pull stream
type PullExpensesRow struct {
    ID               pgtype.UUID
    Category         string
    Description      *string
    Amount           pgtype.Numeric
    ServerReceivedAt pgtype.Timestamptz
    Sequence         int64
}
```

- [ ] **Step 2.3: Verificar que o backend compila**

```powershell
cd backend
go build ./...
```

Expected: sem erros de compilação.

- [ ] **Step 2.4: Commit**

```bash
git add backend/internal/db/queries/events.sql backend/internal/db/gen/
git commit -m "feat(backend/db): queries sqlc para expenses (GetById, Insert, Pull)"
```

---

## Task 3: Backend — Go types (PushEvent + DTOs)

**Files:**
- Modify: `backend/internal/sync/types.go`
- Modify: `backend/internal/sync/pull_dto.go`

- [ ] **Step 3.1: Adicionar `ExpensePayload` e atualizar `PushEvent` em `types.go`**

Substituir o bloco completo de `types.go`:

```go
package sync

import "time"

// PushEvent is one client event in a /sync/push batch. Kind selects the table.
type PushEvent struct {
	Kind            string           `json:"kind"` // sale|restock|stock_adjustment|debt_settlement|expense
	ID              string           `json:"id"`   // client UUID
	ClientCreatedAt time.Time        `json:"client_created_at"`
	Sale            *SalePayload     `json:"sale,omitempty"`
	Restock         *RestockPayload  `json:"restock,omitempty"`
	StockAdjustment *StockAdjPayload `json:"stock_adjustment,omitempty"`
	DebtSettlement  *SettlePayload   `json:"debt_settlement,omitempty"`
	Expense         *ExpensePayload  `json:"expense,omitempty"`
}

type SalePayload struct {
	CustomerID     *string `json:"customer_id"`
	CylinderTypeID string  `json:"cylinder_type_id"`
	Quantity       int     `json:"quantity"`
	UnitPrice      string  `json:"unit_price"`
	CostPrice      string  `json:"cost_price"`
	Total          string  `json:"total"`
	PaymentMethod  string  `json:"payment_method"`
	IsExchange     bool    `json:"is_exchange"`
}

type RestockPayload struct {
	CylinderTypeID string  `json:"cylinder_type_id"`
	Quantity       int     `json:"quantity"`
	CostPerUnit    string  `json:"cost_per_unit"`
	TotalCost      string  `json:"total_cost"`
	Notes          *string `json:"notes"`
}

type StockAdjPayload struct {
	CylinderTypeID string  `json:"cylinder_type_id"`
	Field          string  `json:"field"` // full|empty
	Delta          int     `json:"delta"`
	Reason         *string `json:"reason"`
}

type SettlePayload struct {
	CustomerID    string `json:"customer_id"`
	Amount        string `json:"amount"`
	PaymentMethod string `json:"payment_method"`
}

type ExpensePayload struct {
	Category    string  `json:"category"`
	Description *string `json:"description"`
	Amount      string  `json:"amount"` // decimal string
}

// PushResult is the per-event outcome returned to the client.
type PushResult struct {
	ID               string     `json:"id"`
	Status           string     `json:"status"` // applied|duplicate|error
	Sequence         *int64     `json:"sequence,omitempty"`
	ServerReceivedAt *time.Time `json:"server_received_at,omitempty"`
	Error            string     `json:"error,omitempty"`
}
```

- [ ] **Step 3.2: Adicionar `ExpenseDTO` e `mapExpenseRow` em `pull_dto.go`**

Adicionar ao final do arquivo (antes do último `}`... na verdade é o fim do arquivo sem chave, só adicionar):

```go
// ExpenseDTO is the read-side DTO for expense events.
type ExpenseDTO struct {
	ID               string  `json:"id"`
	Category         string  `json:"category"`
	Description      *string `json:"description"`
	Amount           string  `json:"amount"` // decimal string
	ServerReceivedAt string  `json:"server_received_at"`
	Sequence         int64   `json:"sequence"`
}

func mapExpenseRow(r gen.PullExpensesRow) ExpenseDTO {
	return ExpenseDTO{
		ID:               uuidToWire(r.ID),
		Category:         r.Category,
		Description:      r.Description,
		Amount:           numericToWire(r.Amount),
		ServerReceivedAt: timestamptzToWire(r.ServerReceivedAt),
		Sequence:         r.Sequence,
	}
}
```

- [ ] **Step 3.3: Verificar compilação**

```powershell
cd backend
go build ./...
```

Expected: sem erros.

- [ ] **Step 3.4: Commit**

```bash
git add backend/internal/sync/types.go backend/internal/sync/pull_dto.go
git commit -m "feat(backend/sync): ExpensePayload em PushEvent e ExpenseDTO para pull"
```

---

## Task 4: Backend — push.go

**Files:**
- Modify: `backend/internal/sync/push.go`

- [ ] **Step 4.1: Adicionar "expense" ao switch em `existingHash`**

Localizar o switch em `existingHash` e adicionar o case antes do `return "", false, errors.New("unknown kind")`:

```go
case "expense":
    row, err := q.GetExpenseByID(ctx, mustUUID(e.ID))
    return scanHash(row.PayloadHash, err)
```

O switch completo em `existingHash` fica:

```go
func (s *Service) existingHash(ctx context.Context, e PushEvent) (string, bool, error) {
	q := gen.New(s.pool)
	switch e.Kind {
	case "sale":
		row, err := q.GetSaleByID(ctx, mustUUID(e.ID))
		return scanHash(row.PayloadHash, err)
	case "restock":
		row, err := q.GetRestockByID(ctx, mustUUID(e.ID))
		return scanHash(row.PayloadHash, err)
	case "stock_adjustment":
		row, err := q.GetStockAdjustmentByID(ctx, mustUUID(e.ID))
		return scanHash(row.PayloadHash, err)
	case "debt_settlement":
		row, err := q.GetDebtSettlementByID(ctx, mustUUID(e.ID))
		return scanHash(row.PayloadHash, err)
	case "expense":
		row, err := q.GetExpenseByID(ctx, mustUUID(e.ID))
		return scanHash(row.PayloadHash, err)
	}
	return "", false, errors.New("unknown kind")
}
```

- [ ] **Step 4.2: Adicionar case "expense" em `applyEvent`**

Localizar o switch em `applyEvent` e adicionar o case antes do `return 0, time.Time{}, errors.New("unknown kind")`:

```go
case "expense":
    p := e.Expense
    ins, err := q.InsertExpense(ctx, gen.InsertExpenseParams{
        ID:              mustUUID(e.ID),
        Category:        p.Category,
        Description:     p.Description,
        Amount:          numeric(p.Amount),
        PayloadHash:     hash,
        CreatedBy:       userID,
        ClientCreatedAt: timestamptz(e.ClientCreatedAt),
    })
    if err != nil {
        return 0, time.Time{}, err
    }
    return ins.Sequence, toTime(ins.ServerReceivedAt), nil
```

- [ ] **Step 4.3: Verificar compilação**

```powershell
cd backend
go build ./...
```

Expected: sem erros.

- [ ] **Step 4.4: Commit**

```bash
git add backend/internal/sync/push.go
git commit -m "feat(backend/sync): push handler para expenses"
```

---

## Task 5: Backend — pull.go

**Files:**
- Modify: `backend/internal/sync/pull.go`

- [ ] **Step 5.1: Adicionar `Expense int64` à struct `Cursor`**

Localizar a struct `Cursor` e adicionar o campo:

```go
type Cursor struct {
	Sale    int64 `json:"sale"`
	Restock int64 `json:"restock"`
	Adjust  int64 `json:"adjust"`
	Settle  int64 `json:"settle"`
	Void    int64 `json:"void"`
	Catalog int64 `json:"catalog"`
	Expense int64 `json:"expense"`
}
```

- [ ] **Step 5.2: Adicionar query PullExpenses no método `Pull`**

Localizar onde os outros streams são buscados (após o bloco de `catalogEvts`) e adicionar antes do `sort.SliceStable`:

```go
	expenses, err := q.PullExpenses(ctx, gen.PullExpensesParams{Sequence: c.Expense, Limit: limit})
	if err != nil {
		return PullPage{}, err
	}
	if int32(len(expenses)) == limit {
		anyFull = true
	}
	for _, r := range expenses {
		events = append(events, Event{Kind: "expense", Sequence: r.Sequence, ServerReceivedAt: toTime(r.ServerReceivedAt), Data: mapExpenseRow(r)})
	}
```

- [ ] **Step 5.3: Adicionar avanço do cursor para "expense"**

Localizar o switch de avanço do cursor (após o `sort.SliceStable`) e adicionar o case:

```go
		case "expense":
			if e.Sequence > next.Expense {
				next.Expense = e.Sequence
			}
```

O switch completo do avanço do cursor fica:

```go
	for _, e := range events {
		switch e.Kind {
		case "sale":
			if e.Sequence > next.Sale { next.Sale = e.Sequence }
		case "restock":
			if e.Sequence > next.Restock { next.Restock = e.Sequence }
		case "stock_adjustment":
			if e.Sequence > next.Adjust { next.Adjust = e.Sequence }
		case "debt_settlement":
			if e.Sequence > next.Settle { next.Settle = e.Sequence }
		case "void_sale":
			if e.Sequence > next.Void { next.Void = e.Sequence }
		case "customer_upsert", "customer_delete", "cylinder_upsert":
			if e.Sequence > next.Catalog { next.Catalog = e.Sequence }
		case "expense":
			if e.Sequence > next.Expense { next.Expense = e.Sequence }
		}
	}
```

- [ ] **Step 5.4: Verificar compilação**

```powershell
cd backend
go build ./...
```

Expected: sem erros.

- [ ] **Step 5.5: Commit**

```bash
git add backend/internal/sync/pull.go
git commit -m "feat(backend/sync): pull stream para expenses com cursor independente"
```

---

## Task 6: Backend — testes

**Files:**
- Modify: `backend/internal/sync/testutil_test.go`
- Create: `backend/internal/sync/expense_test.go`

- [ ] **Step 6.1: Adicionar `0006_expenses.up.sql` ao `applyMigrations` em testutil_test.go**

Localizar o slice de migrations em `applyMigrations` e adicionar a nova migration:

```go
func applyMigrations(t *testing.T, pool *pgxpool.Pool) {
	t.Helper()
	ctx := context.Background()
	for _, name := range []string{
		"0001_init.up.sql",
		"0002_sync_errors.up.sql",
		"0004_sale_voids.up.sql",
		"0005_catalog_events.up.sql",
		"0006_expenses.up.sql",
	} {
		path := filepath.Join("..", "db", "migrations", name)
		sql := readFile(t, path)
		if _, err := pool.Exec(ctx, sql); err != nil {
			t.Fatalf("migrate %s: %v", name, err)
		}
	}
}
```

- [ ] **Step 6.2: Criar `backend/internal/sync/expense_test.go`**

```go
package sync

import (
	"context"
	"testing"
	"time"
)

func expenseEvent(id, category, amount string) PushEvent {
	return PushEvent{
		Kind:            "expense",
		ID:              id,
		ClientCreatedAt: time.Now(),
		Expense: &ExpensePayload{
			Category: category,
			Amount:   amount,
		},
	}
}

func TestPush_AppliesExpense(t *testing.T) {
	pool := newTestDB(t)
	svc := NewService(pool)

	ev := expenseEvent("eeeeeeee-0000-0000-0000-000000000001", "Gasolina", "150.00")
	res, err := svc.Push(context.Background(), seedUser, []PushEvent{ev})
	if err != nil {
		t.Fatalf("Push: %v", err)
	}
	if res[0].Status != "applied" {
		t.Fatalf("want applied, got %s (%s)", res[0].Status, res[0].Error)
	}

	var category string
	var amount float64
	pool.QueryRow(context.Background(),
		`SELECT category, amount FROM expenses WHERE id=$1`,
		mustUUID("eeeeeeee-0000-0000-0000-000000000001"),
	).Scan(&category, &amount)

	if category != "Gasolina" {
		t.Fatalf("want category Gasolina, got %s", category)
	}
	if amount != 150.00 {
		t.Fatalf("want amount 150.00, got %v", amount)
	}
}

func TestPush_ExpenseDuplicateIsIdempotent(t *testing.T) {
	pool := newTestDB(t)
	svc := NewService(pool)

	ev := expenseEvent("eeeeeeee-0000-0000-0000-000000000002", "Pneu", "80.00")
	_, _ = svc.Push(context.Background(), seedUser, []PushEvent{ev})
	res, _ := svc.Push(context.Background(), seedUser, []PushEvent{ev})

	if res[0].Status != "duplicate" {
		t.Fatalf("want duplicate, got %s", res[0].Status)
	}

	var count int
	pool.QueryRow(context.Background(),
		`SELECT COUNT(*) FROM expenses WHERE id=$1`,
		mustUUID("eeeeeeee-0000-0000-0000-000000000002"),
	).Scan(&count)
	if count != 1 {
		t.Fatalf("want 1 row, got %d (duplicate was inserted)", count)
	}
}

func TestPull_IncludesExpenses(t *testing.T) {
	pool := newTestDB(t)
	svc := NewService(pool)

	// Push uma despesa
	ev := expenseEvent("eeeeeeee-0000-0000-0000-000000000003", "Manutenção", "200.00")
	res, _ := svc.Push(context.Background(), seedUser, []PushEvent{ev})
	if res[0].Status != "applied" {
		t.Fatalf("setup push failed: %s", res[0].Status)
	}

	// Pull com cursor zero — deve conter a despesa
	page, err := svc.Pull(context.Background(), Cursor{}, 200)
	if err != nil {
		t.Fatalf("Pull: %v", err)
	}

	var found bool
	for _, e := range page.Events {
		if e.Kind == "expense" {
			dto, ok := e.Data.(ExpenseDTO)
			if !ok {
				t.Fatalf("data is not ExpenseDTO")
			}
			if dto.Category == "Manutenção" && dto.Amount == "200.00" {
				found = true
			}
		}
	}
	if !found {
		t.Fatal("expense not found in pull stream")
	}

	// Cursor avançou: re-pull com next_cursor não deve repetir
	page2, _ := svc.Pull(context.Background(), page.NextCursor, 200)
	for _, e := range page2.Events {
		if e.Kind == "expense" {
			t.Fatal("expense appeared twice in pull stream")
		}
	}
}
```

- [ ] **Step 6.3: Rodar testes do pacote sync**

```powershell
cd backend
go test ./internal/sync/ -count=1 -v -run TestPush_Applies -run TestPush_Expense -run TestPull_Includes 2>&1 | Select-String "PASS|FAIL|---"
```

Expected: todos os testes `PASS`.

- [ ] **Step 6.4: Rodar suite completa do pacote sync**

```powershell
cd backend
go test ./internal/sync/ -count=1 -timeout 120s 2>&1 | tail -5
```

Expected: `ok  github.com/pedrogomesdev/gas-manager-backend/internal/sync`

- [ ] **Step 6.5: Commit**

```bash
git add backend/internal/sync/testutil_test.go backend/internal/sync/expense_test.go
git commit -m "test(backend/sync): testes de push + pull para expenses"
```

---

## Task 7: Deploy do backend

- [ ] **Step 7.1: Usar o skill de deploy**

Invocar o skill `/backend` para fazer o deploy no Cloud Run.

O skill roda:
```powershell
gcloud builds submit --config backend/cloudbuild.yaml --project gas-manager-499616
gcloud run deploy gas-backend --source backend --region southamerica-east1 --project gas-manager-499616 --quiet
```

Aguardar confirmação de revisão ativa no Cloud Run antes de continuar.

---

## Task 8: Frontend — tipos (lib/api.ts + types/index.ts)

**Files:**
- Modify: `lib/api.ts`
- Modify: `types/index.ts`

- [ ] **Step 8.1: Adicionar `ExpensePayload` e atualizar `PushEvent` em `lib/api.ts`**

Após a interface `SettlePayload`, adicionar:

```ts
export interface ExpensePayload {
  category: string;
  description: string | null;
  amount: string; // decimal string
}
```

E atualizar `PushEvent`:

```ts
export interface PushEvent {
  kind: "sale" | "restock" | "stock_adjustment" | "debt_settlement" | "expense";
  id: string; // client UUID
  client_created_at: string; // RFC3339
  sale?: SalePayload;
  restock?: RestockPayload;
  stock_adjustment?: StockAdjPayload;
  debt_settlement?: SettlePayload;
  expense?: ExpensePayload;
}
```

- [ ] **Step 8.2: Adicionar interface `Expense` em `types/index.ts`**

Antes de `DashboardStats`:

```ts
export interface Expense {
  id: number;
  uuid: string;
  category: string;
  description: string | null;
  amount: number;
  created_at: string;
}
```

- [ ] **Step 8.3: Rodar testes para garantir que não quebrou nada**

```bash
npx jest --no-coverage
```

Expected: todos passando.

- [ ] **Step 8.4: Commit**

```bash
git add lib/api.ts types/index.ts
git commit -m "feat(types): ExpensePayload em PushEvent e interface Expense"
```

---

## Task 9: Frontend — Migration v5 (SQLite)

**Files:**
- Modify: `db/database.ts`
- Modify: `db/__tests__/migration.test.ts`

- [ ] **Step 9.1: Atualizar `SCHEMA_VERSION` e adicionar bloco v5 em `db/database.ts`**

```ts
export const SCHEMA_VERSION = 5;
```

Após o bloco `if (current < 4)`, adicionar:

```ts
  if (current < 5) {
    await db.withTransactionAsync(async () => {
      await db.execAsync(`
        CREATE TABLE IF NOT EXISTS expenses (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          uuid TEXT NOT NULL UNIQUE,
          category TEXT NOT NULL,
          description TEXT,
          amount REAL NOT NULL,
          created_at TEXT DEFAULT (datetime('now', 'localtime'))
        );
        CREATE UNIQUE INDEX IF NOT EXISTS idx_expenses_uuid ON expenses(uuid);

        PRAGMA user_version = 5;
      `);
    });
  }
```

- [ ] **Step 9.2: Atualizar testes de migration em `db/__tests__/migration.test.ts`**

Substituir todas as ocorrências de `.toBe(4)` por `.toBe(5)` (três lugares: describe "v4", idempotent test, e o describe "v2" que também testa a versão final).

Também atualizar o nome do describe e adicionar verificação da nova tabela:

```ts
describe("schema migration to v5", () => {
  it("brings a fresh database to version 5 with all expected tables and columns", async () => {
    const db = createTestDb();
    await initDatabase(db);

    expect(await userVersion(db)).toBe(5);

    // v3 additions
    expect(await tableExists(db, "applied_events")).toBe(true);
    // v4 additions
    expect(await tableExists(db, "debt_settlements")).toBe(true);
    // v5 additions
    expect(await tableExists(db, "expenses")).toBe(true);
    expect(await columnNames(db, "expenses")).toEqual(
      expect.arrayContaining(["uuid", "category", "description", "amount"])
    );
  });
});
```

E nos outros dois testes que verificam `user_version`:

```ts
// No teste "is idempotent"
expect(await userVersion(db)).toBe(5);
// No teste "brings a fresh database to version 3 (was 2)"
expect(await userVersion(db)).toBe(5);
```

- [ ] **Step 9.3: Rodar testes de migration**

```bash
npx jest db/__tests__/migration.test.ts --no-coverage
```

Expected: 7 testes passando.

- [ ] **Step 9.4: Commit**

```bash
git add db/database.ts db/__tests__/migration.test.ts
git commit -m "feat(db): migration v5 — tabela expenses no SQLite"
```

---

## Task 10: Frontend — db/queries/expenses.ts + store

**Files:**
- Create: `db/queries/expenses.ts`
- Modify: `store/index.ts`

- [ ] **Step 10.1: Criar `db/queries/expenses.ts`**

```ts
import { SQLiteDatabase } from "expo-sqlite";
import { randomUUID } from "expo-crypto";
import { enqueue } from "@/lib/sync/outbox";
import { Expense } from "@/types";

export async function addExpense(
  db: SQLiteDatabase,
  data: { category: string; description?: string; amount: number }
): Promise<void> {
  const uuid = randomUUID();
  const now = new Date().toISOString();

  await db.withTransactionAsync(async () => {
    await db.runAsync(
      `INSERT INTO expenses (uuid, category, description, amount) VALUES (?, ?, ?, ?)`,
      [uuid, data.category, data.description ?? null, data.amount]
    );

    await enqueue(db, {
      event_uuid: uuid,
      kind: "expense",
      payload: JSON.stringify({
        kind: "expense",
        id: uuid,
        client_created_at: now,
        expense: {
          category: data.category,
          description: data.description ?? null,
          amount: data.amount.toFixed(2),
        },
      }),
      client_created_at: now,
    });
  });
}

export async function getExpenses(
  db: SQLiteDatabase,
  from: string,
  to: string
): Promise<Expense[]> {
  return db.getAllAsync<Expense>(
    `SELECT * FROM expenses
     WHERE date(created_at) BETWEEN ? AND ?
     ORDER BY created_at DESC`,
    [from, to]
  );
}
```

- [ ] **Step 10.2: Escrever teste para `addExpense`**

Criar `db/__tests__/expenses.test.ts`:

```ts
import { initDatabase } from "@/db/database";
import { createTestDb } from "@/db/__tests__/helpers/testdb";
import { addExpense, getExpenses } from "@/db/queries/expenses";

async function freshDb() {
  const db = createTestDb();
  await initDatabase(db);
  return db;
}

describe("addExpense", () => {
  it("insere na tabela expenses e enfileira evento no outbox", async () => {
    const db = await freshDb();
    await addExpense(db, { category: "Gasolina", amount: 150 });

    const expense = await db.getFirstAsync<{ uuid: string; category: string; amount: number }>(
      `SELECT uuid, category, amount FROM expenses LIMIT 1`
    );
    expect(expense?.category).toBe("Gasolina");
    expect(expense?.amount).toBeCloseTo(150, 5);
    expect(expense?.uuid).toHaveLength(36);

    const outbox = await db.getFirstAsync<{ kind: string; payload: string }>(
      `SELECT kind, payload FROM sync_outbox WHERE status = 'pending'`
    );
    expect(outbox?.kind).toBe("expense");
    const p = JSON.parse(outbox!.payload);
    expect(p.expense.amount).toBe("150.00");
    expect(p.expense.category).toBe("Gasolina");
  });

  it("getExpenses retorna despesas do periodo", async () => {
    const db = await freshDb();
    await addExpense(db, { category: "Pneu", amount: 80 });

    const today = new Date().toISOString().slice(0, 10);
    const rows = await getExpenses(db, today, today);
    expect(rows).toHaveLength(1);
    expect(rows[0].category).toBe("Pneu");
  });
});
```

- [ ] **Step 10.3: Rodar o teste — esperar PASS**

```bash
npx jest db/__tests__/expenses.test.ts --no-coverage
```

Expected: 2 testes passando.

- [ ] **Step 10.4: Adicionar `expensesVersion` ao `store/index.ts`**

```ts
import { create } from "zustand";

interface AppStore {
  salesVersion: number;
  inventoryVersion: number;
  customersVersion: number;
  expensesVersion: number;
  bumpSales: () => void;
  bumpInventory: () => void;
  bumpCustomers: () => void;
  bumpExpenses: () => void;
}

export const useAppStore = create<AppStore>((set) => ({
  salesVersion: 0,
  inventoryVersion: 0,
  customersVersion: 0,
  expensesVersion: 0,
  bumpSales: () => set((s) => ({ salesVersion: s.salesVersion + 1 })),
  bumpInventory: () => set((s) => ({ inventoryVersion: s.inventoryVersion + 1 })),
  bumpCustomers: () => set((s) => ({ customersVersion: s.customersVersion + 1 })),
  bumpExpenses: () => set((s) => ({ expensesVersion: s.expensesVersion + 1 })),
}));
```

- [ ] **Step 10.5: Rodar a suíte completa**

```bash
npx jest --no-coverage
```

Expected: todos passando.

- [ ] **Step 10.6: Commit**

```bash
git add db/queries/expenses.ts db/__tests__/expenses.test.ts store/index.ts
git commit -m "feat(expenses): query addExpense + getExpenses + expensesVersion no store"
```

---

## Task 11: Frontend — lib/sync (engine + apply)

**Files:**
- Modify: `lib/sync/engine.ts`
- Modify: `lib/sync/apply.ts`

- [ ] **Step 11.1: Adicionar "expense" a `FACT_KINDS` em `lib/sync/engine.ts`**

Substituir a linha da const:

```ts
const FACT_KINDS = new Set([
  "sale",
  "restock",
  "stock_adjustment",
  "debt_settlement",
  "expense",
]);
```

- [ ] **Step 11.2: Adicionar `PulledExpense` e `applyExpense` em `lib/sync/apply.ts`**

No bloco de interfaces (após `PulledVoidSale`), adicionar:

```ts
interface PulledExpense {
  id: string;
  category: string;
  description: string | null;
  amount: string;
  server_received_at: string;
}
```

No `switch` de `applyEvent`, adicionar:

```ts
    case "expense":
      return applyExpense(db, event.data as PulledExpense);
```

No final do arquivo (após `applyCylinderUpsert`), adicionar:

```ts
async function applyExpense(db: SQLiteDatabase, d: PulledExpense): Promise<void> {
  // INSERT OR IGNORE — dedupe por uuid (idêntico ao do push local).
  await db.runAsync(
    `INSERT OR IGNORE INTO expenses (uuid, category, description, amount)
     VALUES (?, ?, ?, ?)`,
    [d.id, d.category, d.description ?? null, parseFloat(d.amount)]
  );
}
```

- [ ] **Step 11.3: Escrever teste para `applyExpense`**

Em `lib/sync/__tests__/apply.test.ts`, adicionar um novo describe ao final do arquivo:

```ts
describe("applyEvent — expense", () => {
  it("insere despesa na tabela local", async () => {
    const db = await freshDb();

    await applyEvent(db, {
      kind: "expense",
      sequence: 1,
      server_received_at: new Date().toISOString(),
      data: {
        id: "exp-uuid-0001",
        category: "Gasolina",
        description: null,
        amount: "150.00",
        server_received_at: new Date().toISOString(),
      },
    });

    const row = await db.getFirstAsync<{
      uuid: string;
      category: string;
      amount: number;
    }>(`SELECT * FROM expenses WHERE uuid = 'exp-uuid-0001'`);

    expect(row).toBeTruthy();
    expect(row!.category).toBe("Gasolina");
    expect(row!.amount).toBeCloseTo(150, 5);
  });

  it("idempotente: mesmo uuid não duplica", async () => {
    const db = await freshDb();

    const ev = {
      kind: "expense" as const,
      sequence: 1,
      server_received_at: new Date().toISOString(),
      data: {
        id: "exp-uuid-0002",
        category: "Pneu",
        description: null,
        amount: "80.00",
        server_received_at: new Date().toISOString(),
      },
    };

    await applyEvent(db, ev);
    await applyEvent(db, ev);

    const count = await db.getFirstAsync<{ c: number }>(
      `SELECT COUNT(*) c FROM expenses WHERE uuid = 'exp-uuid-0002'`
    );
    expect(count?.c).toBe(1);
  });
});
```

- [ ] **Step 11.4: Rodar a suíte completa**

```bash
npx jest --no-coverage
```

Expected: todos passando.

- [ ] **Step 11.5: Commit**

```bash
git add lib/sync/engine.ts lib/sync/apply.ts lib/sync/__tests__/apply.test.ts
git commit -m "feat(sync): expenses no FACT_KINDS e applyExpense no apply.ts"
```

---

## Task 12: Frontend — app/add-expense.tsx

**Files:**
- Create: `app/add-expense.tsx`

- [ ] **Step 12.1: Criar o formulário**

```tsx
import { View, Text, ScrollView, TouchableOpacity, TextInput, Alert } from "react-native";
import { useSQLiteContext } from "expo-sqlite";
import { useState } from "react";
import { router } from "expo-router";
import { addExpense } from "@/db/queries/expenses";
import { useAppStore } from "@/store";

function formatCurrency(value: number) {
  return value.toLocaleString("pt-BR", { style: "currency", currency: "BRL" });
}

const CATEGORIES = ["Gasolina", "Manutenção", "Pneu", "Outros"];

export default function AddExpenseScreen() {
  const db = useSQLiteContext();
  const [category, setCategory] = useState("Gasolina");
  const [customCategory, setCustomCategory] = useState("");
  const [description, setDescription] = useState("");
  const [amount, setAmount] = useState("");
  const [saving, setSaving] = useState(false);
  const bumpExpenses = useAppStore((s) => s.bumpExpenses);

  const isCustom = category === "Outros";
  const finalCategory = isCustom && customCategory.trim()
    ? customCategory.trim()
    : category;

  const handleSave = async () => {
    const value = parseFloat(amount);
    if (!value || value <= 0) return Alert.alert("Erro", "Informe um valor válido");
    if (isCustom && !customCategory.trim()) {
      return Alert.alert("Erro", "Informe a categoria");
    }

    setSaving(true);
    try {
      await addExpense(db, {
        category: finalCategory,
        description: description.trim() || undefined,
        amount: value,
      });
      bumpExpenses();
      router.back();
    } catch (e: any) {
      Alert.alert("Erro", e.message ?? "Falha ao registrar despesa");
    } finally {
      setSaving(false);
    }
  };

  return (
    <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-950" keyboardShouldPersistTaps="handled">
      <View className="px-4 pt-4 gap-4">

        <View>
          <Text className="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Categoria</Text>
          <View className="flex-row flex-wrap gap-2">
            {CATEGORIES.map((cat) => (
              <TouchableOpacity
                key={cat}
                className={`rounded-xl px-4 py-2.5 border ${
                  category === cat
                    ? "bg-primary-500 border-primary-500"
                    : "bg-white dark:bg-gray-900 border-gray-200 dark:border-gray-700"
                }`}
                onPress={() => setCategory(cat)}
              >
                <Text className={`font-semibold text-sm ${category === cat ? "text-white" : "text-gray-700 dark:text-gray-300"}`}>
                  {cat}
                </Text>
              </TouchableOpacity>
            ))}
          </View>
          {isCustom && (
            <TextInput
              className="mt-2 bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-xl px-4 py-3 text-gray-900 dark:text-gray-50"
              placeholder="Nome da categoria..."
              placeholderTextColor="#9ca3af"
              value={customCategory}
              onChangeText={setCustomCategory}
              autoFocus
            />
          )}
        </View>

        <View>
          <Text className="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Valor (R$)</Text>
          <TextInput
            className="bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-xl px-4 py-3 text-gray-900 dark:text-gray-50 text-xl font-bold"
            keyboardType="decimal-pad"
            placeholder="0,00"
            placeholderTextColor="#9ca3af"
            value={amount}
            onChangeText={setAmount}
          />
        </View>

        <View>
          <Text className="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">
            Descrição (opcional)
          </Text>
          <TextInput
            className="bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-xl px-4 py-3 text-gray-900 dark:text-gray-50"
            placeholder="Ex: Posto BR, km 12.400..."
            placeholderTextColor="#9ca3af"
            value={description}
            onChangeText={setDescription}
            multiline
            numberOfLines={2}
          />
        </View>

        <TouchableOpacity
          className={`rounded-xl py-4 items-center mt-2 mb-8 ${saving ? "bg-gray-300 dark:bg-gray-700" : "bg-primary-500"}`}
          onPress={handleSave}
          disabled={saving}
        >
          <Text className="text-white font-bold text-base">
            {saving ? "Salvando..." : "Registrar Despesa"}
          </Text>
        </TouchableOpacity>
      </View>
    </ScrollView>
  );
}
```

- [ ] **Step 12.2: Commit**

```bash
git add app/add-expense.tsx
git commit -m "feat(ui): tela de adicionar despesa com categorias rapidas"
```

---

## Task 13: Frontend — aba Financeiro com seção Despesas

**Files:**
- Modify: `app/(tabs)/reports.tsx`

- [ ] **Step 13.1: Adicionar imports e estado de despesas em `reports.tsx`**

No topo do arquivo, adicionar o import:

```ts
import { getExpenses } from "@/db/queries/expenses";
import { Expense } from "@/types";
```

No componente `ReportsScreen`, adicionar após `const [settlements, setSettlements] = useState`:

```ts
const [expenses, setExpenses] = useState<Expense[]>([]);
```

No `load` callback, adicionar `getExpenses` ao `Promise.all`:

```ts
const [reportRows, dashStats, settleRows, expenseRows] = await Promise.all([
  getReportByPeriod(db, from, to),
  getDashboardStats(db),
  getSettlements(db, from, to),
  getExpenses(db, from, to),
]);
setRows(reportRows as any[]);
setStats(dashStats);
setSettlements(settleRows);
setExpenses(expenseRows);
```

No `useEffect` que chama `load`, adicionar `expensesVersion` às dependências:

```ts
const expensesVersion = useAppStore((s) => s.expensesVersion);
useEffect(() => { load(); }, [load, salesVersion, customersVersion, expensesVersion]);
```

- [ ] **Step 13.2: Adicionar seção "Despesas" na JSX de `reports.tsx`**

Após o fechamento da seção de Vales Recebidos (antes do `rows.length === 0 && settlements.length === 0` check), adicionar:

```tsx
        {/* Despesas */}
        <Text className="text-xs font-bold text-gray-400 dark:text-gray-500 uppercase tracking-widest mb-2">
          Despesas
        </Text>
        {expenses.length > 0 ? (
          <>
            <View className="bg-red-50 dark:bg-red-950 border border-red-200 dark:border-red-800 rounded-2xl p-4 mb-3 flex-row items-center justify-between">
              <View>
                <Text className="text-red-700 dark:text-red-400 font-bold text-xl">
                  {formatCurrency(expenses.reduce((acc, e) => acc + e.amount, 0))}
                </Text>
                <Text className="text-red-600 dark:text-red-500 text-xs mt-0.5">
                  {expenses.length} despesa(s) no período
                </Text>
              </View>
              <Ionicons name="arrow-down-circle" size={32} color="#dc2626" />
            </View>

            <View className="bg-white dark:bg-gray-900 rounded-2xl border border-gray-100 dark:border-gray-800 overflow-hidden mb-4">
              {expenses.map((e, idx) => (
                <View
                  key={e.uuid}
                  className={`px-4 py-3 flex-row items-center justify-between ${
                    idx < expenses.length - 1 ? "border-b border-gray-100 dark:border-gray-800" : ""
                  }`}
                >
                  <View className="flex-1 mr-3">
                    <Text className="font-medium text-gray-900 dark:text-gray-50 text-sm">
                      {e.category}
                    </Text>
                    {e.description && (
                      <Text className="text-xs text-gray-400 dark:text-gray-500 mt-0.5">
                        {e.description}
                      </Text>
                    )}
                    <Text className="text-xs text-gray-400 dark:text-gray-500">
                      {new Date(e.created_at).toLocaleTimeString("pt-BR", {
                        hour: "2-digit",
                        minute: "2-digit",
                      })}
                    </Text>
                  </View>
                  <Text className="font-bold text-red-600 dark:text-red-400">
                    -{formatCurrency(e.amount)}
                  </Text>
                </View>
              ))}
            </View>
          </>
        ) : (
          <View className="bg-white dark:bg-gray-900 rounded-2xl border border-gray-100 dark:border-gray-800 p-6 items-center mb-4">
            <Ionicons name="receipt-outline" size={36} color="#d1d5db" />
            <Text className="text-gray-400 dark:text-gray-500 mt-2 font-medium text-sm">
              Nenhuma despesa {periodLabels[period].toLowerCase()}
            </Text>
            <TouchableOpacity
              className="mt-3 bg-primary-500 rounded-xl px-4 py-2"
              onPress={() => router.push("/add-expense")}
            >
              <Text className="text-white font-semibold text-sm">Registrar despesa</Text>
            </TouchableOpacity>
          </View>
        )}
```

No topo do arquivo, adicionar o import do router se ainda não estiver:

```ts
import { router } from "expo-router";
```

- [ ] **Step 13.3: Adicionar botão de despesa na seção de Vendas (reports.tsx)**

No header da tela (após o title "Financeiro"), adicionar um botão de ação rápida para registrar despesa — **opcionalmente** também no Dashboard (`app/(tabs)/index.tsx`). No `reports.tsx`, logo após o `<Text className="... mb-3">Financeiro</Text>`:

```tsx
<TouchableOpacity
  className="bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-xl px-4 py-3 flex-row items-center mb-4"
  onPress={() => router.push("/add-expense")}
>
  <Ionicons name="remove-circle-outline" size={20} color="#ef4444" />
  <Text className="ml-2 font-semibold text-gray-700 dark:text-gray-300 flex-1">
    Registrar despesa
  </Text>
  <Ionicons name="chevron-forward" size={16} color="#9ca3af" />
</TouchableOpacity>
```

- [ ] **Step 13.4: Rodar a suíte completa**

```bash
npx jest --no-coverage
```

Expected: todos passando.

- [ ] **Step 13.5: Commit**

```bash
git add "app/(tabs)/reports.tsx"
git commit -m "feat(financeiro): secao de despesas na aba Financeiro com botao rapido"
```

---

## Self-Review

**Spec coverage:**
- ✅ Registrar despesa com categoria (Task 12)
- ✅ Sync entre dispositivos — push via outbox + pull via cursor (Tasks 4, 5, 11)
- ✅ Visível no Financeiro por período (Task 13)
- ✅ Backend com idempotência via payload_hash (Tasks 4, 6)
- ✅ TDD: testes backend (Task 6) + testes frontend (Tasks 10, 11)
- ✅ Migration tanto no Postgres (Task 1) quanto no SQLite (Task 9)
- ✅ Dark mode incluído no formulário e na seção do Financeiro

**Não coberto (fora do escopo pedido):**
- Cancelar/anular uma despesa — não foi pedido
- Editar uma despesa — não foi pedido
- Relatório de despesas por categoria ao longo do tempo — não foi pedido

**Type consistency:**
- `Expense` (types/index.ts Task 8) — usada em `db/queries/expenses.ts` (Task 10), `lib/sync/apply.ts` (Task 11), `app/(tabs)/reports.tsx` (Task 13) ✅
- `ExpensePayload` (lib/api.ts Task 8) — referenciada como `PushEvent.expense` em `db/queries/expenses.ts` (Task 10) ✅
- `addExpense(db, {category, description?, amount})` — assinatura consistente entre Task 10 (implementação) e Task 12 (caller) ✅
- `gen.InsertExpenseParams` — baseado no sqlc generate da Task 2; usado em `push.go` Task 4 ✅
- `gen.PullExpensesRow` — baseado no sqlc generate da Task 2; usado em `mapExpenseRow` Task 3 e `Pull()` Task 5 ✅
