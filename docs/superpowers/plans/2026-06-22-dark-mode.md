# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar dark mode automático (baseado na preferência do sistema) a todas as telas do app usando NativeWind.

**Architecture:** `tailwind.config.js` habilita `darkMode: 'media'` — NativeWind automaticamente ativa classes `dark:` quando o sistema está em modo escuro. Header e tab bar são estilizados via `useColorScheme` do react-native (por serem React Navigation, não NativeWind). Sem toggle manual — usa preferência do SO.

**Tech Stack:** NativeWind v4, Expo SDK 54, React Navigation, `useColorScheme` (react-native)

---

## Paleta dark

| Papel | Light | Dark |
|---|---|---|
| Fundo principal | `bg-gray-50` | `dark:bg-gray-950` |
| Card/superfície | `bg-white` | `dark:bg-gray-900` |
| Borda | `border-gray-100/200` | `dark:border-gray-800` |
| Texto principal | `text-gray-900` | `dark:text-gray-50` |
| Texto secundário | `text-gray-500` | `dark:text-gray-400` |
| Texto placeholder | `text-gray-400` | `dark:text-gray-500` |
| Input fundo | `bg-white` | `dark:bg-gray-800` |
| Input borda | `border-gray-200` | `dark:border-gray-700` |
| Header | `#f97316` (laranja) | sem mudança — laranja em ambos |
| Tab bar fundo | `#ffffff` | `#111827` (gray-900) |
| Tab bar borda | `#e5e7eb` | `#1f2937` (gray-800) |

---

## File Map

| Arquivo | Ação |
|---|---|
| `tailwind.config.js` | Habilitar `darkMode: 'media'` |
| `app/(tabs)/_layout.tsx` | Tab bar + header com `useColorScheme` |
| `app/(tabs)/index.tsx` | Dashboard |
| `app/(tabs)/sales.tsx` | Tela de vendas |
| `app/(tabs)/customers.tsx` | Tela de clientes |
| `app/(tabs)/inventory.tsx` | Tela de estoque |
| `app/(tabs)/reports.tsx` | Aba Financeiro |
| `app/sale-form.tsx` | Formulário de venda + modal de cliente |
| `app/settle-debt.tsx` | Tela de quitação |
| `app/customer-form.tsx` | Formulário de cliente |
| `app/customer-detail.tsx` | Detalhe do cliente |
| `app/login.tsx` | Tela de login |
| `app/restock-form.tsx` | Formulário de reabastecimento |

---

## Task 1: Habilitar dark mode no NativeWind

**Files:**
- Modify: `tailwind.config.js`

- [ ] **Step 1.1: Adicionar darkMode**

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./app/**/*.{js,jsx,ts,tsx}",
    "./components/**/*.{js,jsx,ts,tsx}",
  ],
  presets: [require("nativewind/preset")],
  darkMode: "media",
  theme: {
    extend: {
      colors: {
        primary: {
          50:  "#fff7ed",
          100: "#ffedd5",
          200: "#fed7aa",
          300: "#fdba74",
          400: "#fb923c",
          500: "#f97316",
          600: "#ea580c",
          700: "#c2410c",
          800: "#9a3412",
          900: "#7c2d12",
        },
      },
    },
  },
  plugins: [],
};
```

- [ ] **Step 1.2: Commit**

```bash
git add tailwind.config.js
git commit -m "feat(dark): habilitar darkMode media no NativeWind"
```

---

## Task 2: Tab bar e header com esquema de cores dinâmico

**Files:**
- Modify: `app/(tabs)/_layout.tsx`

- [ ] **Step 2.1: Substituir conteúdo completo**

```tsx
import { Tabs, router } from "expo-router";
import { Ionicons } from "@expo/vector-icons";
import { useColorScheme } from "react-native";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { SyncBadge } from "@/components/SyncBadge";
import { signOutUser } from "@/lib/auth";

type IoniconsName = React.ComponentProps<typeof Ionicons>["name"];

function TabIcon({
  name,
  color,
  size,
}: {
  name: IoniconsName;
  color: string | unknown;
  size: number;
}) {
  return <Ionicons name={name} size={size} color={color as string} />;
}

async function handleLogout() {
  await signOutUser();
  router.replace("/login" as any);
}

export default function TabLayout() {
  const insets = useSafeAreaInsets();
  const colorScheme = useColorScheme();
  const dark = colorScheme === "dark";

  return (
    <Tabs
      screenOptions={{
        tabBarActiveTintColor: "#f97316",
        tabBarInactiveTintColor: dark ? "#6b7280" : "#9ca3af",
        tabBarStyle: {
          backgroundColor: dark ? "#111827" : "#ffffff",
          borderTopColor: dark ? "#1f2937" : "#e5e7eb",
          paddingBottom: insets.bottom + 8,
          height: 52 + insets.bottom,
        },
        tabBarLabelStyle: { fontSize: 11, fontWeight: "600" },
        headerStyle: { backgroundColor: "#f97316" },
        headerTintColor: "#ffffff",
        headerTitleStyle: { fontWeight: "700" },
        headerRight: () => <SyncBadge onLogout={handleLogout} />,
      }}
    >
      <Tabs.Screen
        name="index"
        options={{
          title: "Dashboard",
          tabBarIcon: ({ color, size }) => (
            <TabIcon name="home" color={color} size={size} />
          ),
        }}
      />
      <Tabs.Screen
        name="sales"
        options={{
          title: "Vendas",
          tabBarIcon: ({ color, size }) => (
            <TabIcon name="cart" color={color} size={size} />
          ),
        }}
      />
      <Tabs.Screen
        name="inventory"
        options={{
          title: "Estoque",
          tabBarIcon: ({ color, size }) => (
            <TabIcon name="cube" color={color} size={size} />
          ),
        }}
      />
      <Tabs.Screen
        name="customers"
        options={{
          title: "Clientes",
          tabBarIcon: ({ color, size }) => (
            <TabIcon name="people" color={color} size={size} />
          ),
        }}
      />
      <Tabs.Screen
        name="reports"
        options={{
          title: "Financeiro",
          tabBarIcon: ({ color, size }) => (
            <TabIcon name="wallet" color={color} size={size} />
          ),
        }}
      />
    </Tabs>
  );
}
```

- [ ] **Step 2.2: Commit**

```bash
git add app/(tabs)/_layout.tsx
git commit -m "feat(dark): tab bar e header adaptam ao esquema de cores do sistema"
```

---

## Task 3: Dashboard — dark mode

**Files:**
- Modify: `app/(tabs)/index.tsx`

Aplicar as seguintes substituições de classe em todo o arquivo:

| Remover | Adicionar |
|---|---|
| `bg-gray-50` (fundo ScrollView) | `bg-gray-50 dark:bg-gray-950` |
| `bg-white` (cards) | `bg-white dark:bg-gray-900` |
| `border-gray-100` | `border-gray-100 dark:border-gray-800` |
| `text-gray-900` | `text-gray-900 dark:text-gray-50` |
| `text-gray-500` | `text-gray-500 dark:text-gray-400` |
| `text-gray-400` | `text-gray-400 dark:text-gray-500` |
| `bg-red-50 border-red-200` (fiado) | `bg-red-50 dark:bg-red-950 border-red-200 dark:border-red-800` |
| `text-red-700` | `text-red-700 dark:text-red-400` |
| `text-red-500` | `text-red-500 dark:text-red-400` |

- [ ] **Step 3.1: Aplicar as substituições em `app/(tabs)/index.tsx`**

Percorrer o arquivo e adicionar os sufixos `dark:` conforme a tabela acima a cada classe que tiver equivalente. O card do dashboard laranja (faturamento do mês) e o header laranja não mudam.

- [ ] **Step 3.2: Verificar visualmente no app (alternar tema do celular)**

Ir em Configurações do Android → Exibição → Tema escuro ativar → abrir o app → dashboard deve aparecer em escuro.

- [ ] **Step 3.3: Commit**

```bash
git add app/(tabs)/index.tsx
git commit -m "feat(dark): dashboard com dark mode"
```

---

## Task 4: Tela de Vendas — dark mode

**Files:**
- Modify: `app/(tabs)/sales.tsx`

- [ ] **Step 4.1: Aplicar substituições**

Mesma tabela de classes da Task 3. Atenção especial:

- `bg-green-100 text-green-700` (badge cash) → adicionar `dark:bg-green-900 dark:text-green-300`
- `bg-blue-100 text-blue-700` (badge pix) → adicionar `dark:bg-blue-900 dark:text-blue-300`
- `bg-purple-100 text-purple-700` (badge card) → adicionar `dark:bg-purple-900 dark:text-purple-300`
- `bg-red-100 text-red-700` (badge fiado) → adicionar `dark:bg-red-900 dark:text-red-300`
- FAB (botão laranja flutuante) → não muda

- [ ] **Step 4.2: Commit**

```bash
git add app/(tabs)/sales.tsx
git commit -m "feat(dark): tela de vendas com dark mode"
```

---

## Task 5: Tela de Clientes — dark mode

**Files:**
- Modify: `app/(tabs)/customers.tsx`

- [ ] **Step 5.1: Aplicar substituições**

Usar a mesma tabela da Task 3 mais:

- `bg-red-50 border-red-200` (banner devedores) → `bg-red-50 dark:bg-red-950 border-red-200 dark:border-red-800`
- `text-red-700` → `text-red-700 dark:text-red-400`
- `bg-green-100 text-green-700` (badge "Em dia") → `bg-green-100 dark:bg-green-900 text-green-700 dark:text-green-300`
- Input de busca: `bg-white border-gray-200` → `bg-white dark:bg-gray-800 border-gray-200 dark:border-gray-700`
- Placeholder color já é definido via prop JS (`placeholderTextColor="#9ca3af"`) — manter como está

- [ ] **Step 5.2: Commit**

```bash
git add app/(tabs)/customers.tsx
git commit -m "feat(dark): tela de clientes com dark mode"
```

---

## Task 6: Tela de Estoque — dark mode

**Files:**
- Modify: `app/(tabs)/inventory.tsx`

- [ ] **Step 6.1: Aplicar substituições**

Mesma tabela + badges de status de estoque:

- `bg-red-100 text-red-600` (estoque baixo) → `bg-red-100 dark:bg-red-900 text-red-600 dark:text-red-300`
- `bg-yellow-100 text-yellow-700` (médio) → `bg-yellow-100 dark:bg-yellow-900 text-yellow-700 dark:text-yellow-300`
- `bg-green-100 text-green-700` (OK) → `bg-green-100 dark:bg-green-900 text-green-700 dark:text-green-300`

- [ ] **Step 6.2: Commit**

```bash
git add app/(tabs)/inventory.tsx
git commit -m "feat(dark): tela de estoque com dark mode"
```

---

## Task 7: Aba Financeiro — dark mode

**Files:**
- Modify: `app/(tabs)/reports.tsx`

- [ ] **Step 7.1: Aplicar substituições**

Mesma tabela + seção de vales:

- `bg-green-50 border-green-200` (total recebido) → `bg-green-50 dark:bg-green-950 border-green-200 dark:border-green-800`
- `text-green-700` → `text-green-700 dark:text-green-400`
- Period selector: `bg-gray-200` → `bg-gray-200 dark:bg-gray-800`; item ativo `bg-white shadow` → `bg-white dark:bg-gray-700 shadow`

- [ ] **Step 7.2: Commit**

```bash
git add app/(tabs)/reports.tsx
git commit -m "feat(dark): aba Financeiro com dark mode"
```

---

## Task 8: Formulário de Venda + Modal de Cliente — dark mode

**Files:**
- Modify: `app/sale-form.tsx`

- [ ] **Step 8.1: Aplicar substituições**

Atenção especial no modal:

- Modal fundo: `bg-gray-50` → `bg-gray-50 dark:bg-gray-950`
- Header do modal: `bg-white border-gray-100` → `bg-white dark:bg-gray-900 border-gray-100 dark:border-gray-800`
- Search input no modal: `bg-gray-100 border-gray-200` → `bg-gray-100 dark:bg-gray-800 border-gray-200 dark:border-gray-700`
- Items do modal (cards de cliente): mesma regra de card branco
- ScrollView principal: `bg-gray-50` → `bg-gray-50 dark:bg-gray-950`
- Input de preço: `bg-white border-gray-200` → `bg-white dark:bg-gray-800 border-gray-200 dark:border-gray-700`
- Botões de método de pagamento não selecionados: `bg-white border-gray-200 text-gray-700` → adicionar `dark:bg-gray-800 dark:border-gray-700 dark:text-gray-300`
- Toggle de troca: `bg-white border-gray-200` → `bg-white dark:bg-gray-800 border-gray-200 dark:border-gray-700`
- Card do total: `bg-primary-50 border-primary-200` → não muda (laranja)

- [ ] **Step 8.2: Commit**

```bash
git add app/sale-form.tsx
git commit -m "feat(dark): formulário de venda com dark mode"
```

---

## Task 9: settle-debt, customer-form, customer-detail, login, restock-form — dark mode

**Files:**
- Modify: `app/settle-debt.tsx`
- Modify: `app/customer-form.tsx`
- Modify: `app/customer-detail.tsx`
- Modify: `app/login.tsx`
- Modify: `app/restock-form.tsx`

- [ ] **Step 9.1: Aplicar a mesma tabela de substituições em cada arquivo**

Para todas as cinco telas, a regra geral é a mesma tabela da Task 3. Telas específicas:

`login.tsx`: fundo pode ser laranja ou branco — verificar e adicionar `dark:bg-gray-950` no container principal. Inputs: `dark:bg-gray-800 dark:border-gray-700 dark:text-gray-50`.

`customer-detail.tsx`: cards de vendas → regra padrão. Badge de fiado (se houver) → regra de vermelho escuro.

`settle-debt.tsx`: card de dívida `bg-red-50 border-red-200` → `dark:bg-red-950 dark:border-red-800`. Input de valor → `dark:bg-gray-800 dark:border-gray-700`. Botões de método: mesma regra de formulário.

`restock-form.tsx`: inputs e cards → regra padrão.

- [ ] **Step 9.2: Rodar testes para garantir que nenhuma mudança quebrou lógica**

```bash
npx jest --no-coverage
```

Expected: todos passando.

- [ ] **Step 9.3: Testar visualmente — alternar tema no celular e navegar por todas as telas**

Checklist:
- [ ] Dashboard
- [ ] Vendas (incluindo SaleCard)
- [ ] Nova Venda (formulário + modal de cliente)
- [ ] Estoque
- [ ] Clientes (incluindo badges)
- [ ] Quitar fiado
- [ ] Financeiro (todas as seções)
- [ ] Login

- [ ] **Step 9.4: Commit**

```bash
git add app/settle-debt.tsx app/customer-form.tsx app/customer-detail.tsx app/login.tsx app/restock-form.tsx
git commit -m "feat(dark): dark mode em todas as telas restantes"
```

---

## Self-Review

**Spec coverage:**
- ✅ Dark mode baseado na preferência do sistema (não manual)
- ✅ Tab bar e header adaptam via JS `useColorScheme` (não NativeWind)
- ✅ Todas as telas cobertas
- ✅ Paleta consistente definida no início do plano

**Não coberto:**
- Toggle manual de dark mode dentro do app — não foi pedido
- SyncBadge component — verificar se tem classes que precisam de dark: (simples, provavelmente só ícones e texto)
